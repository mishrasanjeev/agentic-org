version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: agentflow
      POSTGRES_USER: agentflow
      POSTGRES_PASSWORD: agentflow_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agentflow"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    command: redis-server --appendonly yes

  minio:
    image: minio/minio:latest
    environment:
      MINIO_ROOT_USER: agentflow
      MINIO_ROOT_PASSWORD: agentflow_dev
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      AGENTFLOW_ENV: development
      AGENTFLOW_DB_URL: postgresql+asyncpg://agentflow:agentflow_dev@postgres:5432/agentflow
      AGENTFLOW_REDIS_URL: redis://redis:6379/0
      AGENTFLOW_STORAGE_BUCKET: agentflow-docs-dev
      AGENTFLOW_STORAGE_ENDPOINT: http://minio:9000
      AGENTFLOW_SECRET_KEY: dev-secret-key-change-in-production-32chars
      AGENTFLOW_PII_MASKING: "true"
      AGENTFLOW_LOG_LEVEL: DEBUG
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    volumes:
      - .:/app
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      AGENTFLOW_ENV: development
      AGENTFLOW_DB_URL: postgresql+asyncpg://agentflow:agentflow_dev@postgres:5432/agentflow
      AGENTFLOW_REDIS_URL: redis://redis:6379/0
      AGENTFLOW_STORAGE_BUCKET: agentflow-docs-dev
      AGENTFLOW_STORAGE_ENDPOINT: http://minio:9000
      AGENTFLOW_SECRET_KEY: dev-secret-key-change-in-production-32chars
      AGENTFLOW_PII_MASKING: "true"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: celery -A core.worker worker --loglevel=info --concurrency=4

volumes:
  pgdata:
  redisdata:
  miniodata:
