# 2026-05-17 Post-Deploy Follow-Up

Production deploy of main commit `46798ad20c82ee02cdf76e362f16f92c7163c081` completed with warnings.

## Deployed Revisions

- API revision: `agenticorg-api-00064-tqc` with 100% traffic.
- UI revision: `agenticorg-ui-00033-vvx` with 100% traffic.
- Migration job: `agenticorg-migrate-988ks`, completed successfully.
- Rollback API revision: `agenticorg-api-00058-r4p`.
- Previous serving commit discovered during deploy: `7cb8504c20af5b1f9d3bc23fbacc20b4ad7f6117`.

## Verified Invariants

- `routes_missing_metadata: 0`
- `process_local_allowed: 0`
- `process_local_blocked: 0`
- `broad_exceptions_allowed: 22`
- Alembic head: `v4916_merge_p0_heads`

## Warnings

- UI Cloud Run metadata still reported stale `GIT_SHA=93ca419...` even though the deployed UI image tag and digest came from commit `46798ad`.
- Authenticated production smoke checks were skipped because no approved smoke account, session, tenant, or signed test-event secret was available.

## Follow-Ups

- Update the official Cloud Run deploy script so the UI service gets `GIT_SHA` set to the same deploy SHA as the image tag.
- Add an env-driven production smoke script that runs authenticated checks only with approved smoke credentials and reports missing credentials as explicit `SKIPPED` results.

This note is documentary only and does not change production traffic.
