# ==============================================================================
# AgenticOrg v4.0.0 — Optional v4 Feature Dependencies
# ==============================================================================
#
# These enable v4-specific features. The platform runs without them
# (all imports are guarded with try/except — graceful degradation).
#
# Install: pip install -r requirements-v4.txt
#
# ==============================================================================

# Composio Marketplace is not installed in the production v4 dependency
# set until composio-core supports Pillow>=12.2.0. The API remains
# guarded and returns 503 when the SDK is unavailable.

# ─── RouteLLM: Smart Multi-Model Routing (Apache 2.0) ───────────────────────
# Removed from production v4 deps on 2026-06-13: routellm==0.2.0
# depends on torch, and pip-audit reports torch 2.12.0 with
# CVE-2025-3000 and no fixed version. The LLM router already falls
# back to the built-in heuristic path when routellm is absent.

# ─── Microsoft Presidio: Pre-LLM PII Redaction (MIT) ────────────────────────
# Enables: Aadhaar, PAN, GSTIN, UPI, email, phone scrubbed BEFORE reaching LLM
# Without: PII masking in logs only (post-LLM)
# Python 3.14 is the production Docker runtime. presidio-analyzer
# 2.2.360+ currently requires Python<3.14, so pin the newest
# compatible release for production and keep the newer line for
# Python 3.12/3.13 environments.
presidio-analyzer==2.2.359; python_version >= "3.14"
presidio-analyzer==2.2.363; python_version < "3.14"
# presidio-anonymizer 2.2.363 pins cryptography<47.0.0, conflicting with
# grantex>=0.3.13 and the cryptography>=48.0.1 security floor.
presidio-anonymizer==2.2.362
